class DateTime{constructor(date,zone){this.date=new Date(date);this.zone=zone;}static now(){const ms=Settings.now?Settings.now():Date.now();return new DateTime(ms);}setZone(zone){const d=new Date(this.date.toLocaleString('en-US',{timeZone:zone}));return new DateTime(d,zone);}set(o){const d=new Date(this.date);if(o.hour!==undefined)d.setHours(o.hour);if(o.minute!==undefined)d.setMinutes(o.minute);if(o.second!==undefined)d.setSeconds(o.second);if(o.millisecond!==undefined)d.setMilliseconds(o.millisecond);return new DateTime(d,this.zone);}minus(o){const d=new Date(this.date);if(o.days)d.setDate(d.getDate()-o.days);return new DateTime(d,this.zone);}diff(other,unit){const ms=this.date-other.date;if(unit==='seconds')return{seconds:ms/1000};return{milliseconds:ms};}valueOf(){return this.date.getTime();}}
const Settings={now:null};
window.luxon={DateTime,Settings};
